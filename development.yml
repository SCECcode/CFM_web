# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    ports:
      - 5434:5432
    restart: unless-stopped
      
  web:
    volumes:
      - ./:/app

  altweb:
    volumes:
      - ./:/app

  rupweb:
    volumes:
      - ./:/app

  old53web:
    volumes:
      - ./:/app


